# codexa
Codexa is a CLI tool that analyzes multi-repo or monorepo codebases and auto-generates structured markdown context files for each module and directory. It extracts file summaries, dependency graphs, non-obvious patterns, and tribal knowledge hints using a configurable LLM backend (OpenAI-compatible). The resulting CODEXA.md files per directory help
